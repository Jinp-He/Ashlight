# 角色卡牌设计规则书 v2

> 状态：grillme 拷问产出（2026-07-05）；**2026-07-10 对账修订**——与代码逐条对账后按拍板结论更新了 §1.2 手牌（弃光重抽）、§2.1 速度（法4/战3/游2）、§3/§4.1 执行牌（真延迟、结算不开新回合）、§4.2（战士可有执行牌）、§5.1 过载（双通道）、§7 待办。
> 在 [[职业设计_三职业与公用系统]] v1 基础上收敛出的**可落地设计框架**。
> 用途：作为三套起手卡组、数值调优、以及执行线补齐的共同基准。
> 相关：`Assets/Scripts/Battle/*`、card-effect-authoring skill。

---

## 0. 先分清两条线

整个卡牌工作拆成互不阻塞的两条线：

1. **设计线（本文）**——每张卡"是什么、花多少、打出来给谁什么效果"。设计时**按理想中的完整机制来写**，不管它现在实现没实现。
2. **执行线（phase-2）**——卡牌效果在战斗里**真正跑起来**。有一批签名机制目前是空的或接错了系统，清单见 §7。

> 一句话：设计线可以先跑在执行线前面。设计里写"移动获得闪避"，哪怕闪避现在还没被消费，也照写；补实现是执行线的活。

---

## 1. 经济系统：能量与手牌

### 1.1 能量池（各自独立）

- 三名英雄同时参战，**每人一个独立能量池**，轮到自己时把能量花完为止，出牌张数不设硬上限。
- 池子大小按职业分：

  | 职业 | 每回合能量池 |
  |---|---|
  | 法师 | **4** |
  | 战士 | **3** |
  | 游侠 | **2** |

- **决定"每回合打几张"的是卡的单价,不是池子大小**。池子只是"这个职业一回合大概花得起多少钱"的容器：
  - 法师池 4 = 一张 3~4 费的大执行牌（＋可能一张 1 费小支援）。
  - 战士池 3 = 两个 1~2 费的稳定动作。
  - 游侠池 2 = 主要用来付**移动**的钱；攻击牌（小刀）几乎都是 0 费,所以出牌数量由手牌/造刀决定,而非能量。

> ⚠️ 游侠的能量是**移动预算**,不是攻击预算。这条锁死了一个约束：**游侠的非移动牌基本都得是 0 费**,否则池 2 会瞬间卡死。

### 1.2 手牌（各自独立）

- 每人**独立的抽牌堆、独立的手牌、独立的弃牌堆**。每人只用自己的牌,互不干涉。
- 每回合开始抽 **5 张**；**回合结束弃光全部手牌**,下回合重抽（不留牌、不囤牌）。（07-10 拍板：以代码现状为准,原「打不出的留在手里」作废——法师"攒答案"改为靠抽牌一致性与卡池密度实现,不靠囤手牌。）

> 备注：这与旧文档 v1 里"手牌全队共享"的说法不同——实际做下来是**各管各的手牌**,更干净,也不存在"快的角色把公共手牌占满"的问题。

---

## 2. 行动频率：速度

### 2.1 速度 = 行动间隔（值越大越慢）

- 战斗是**离散回合制**：所有单位在一条队列上排队,轮到谁谁行动,行动完按自己的**速度间隔**重新排到后面。
- **注意语义反直觉**：引擎里 `Speed` 数值其实是"间隔格数",**数值越大 = 隔越久才轮到 = 回合越少**。它更像"冷却",不是"手速"。
- 排序：**法师最慢 / 战士居中 / 游侠最快**。定案数值（07-10 拍板并已落表）：**法师 4 / 战士 3 / 游侠 2**。

  > ⚠️ 速度改动使 [[起手卡组设计_费用模型对齐]] §1 的每手番预算（按 游3/战4/法5 标定）失效,扩池前需按新速度重跑一遍定价。

### 2.2 吞吐量 = 能量池 × 频率

- 这是整套数值的平衡等式：
  - 法师：**大池子 × 少回合** —— 攒一发大的,难得动一次。
  - 游侠：**小池子 × 多回合** —— 每次花得少,但动得勤。
  - 战士：两者都居中。
- 三个人拉长了看,总产出大致相当;区别只在**手感节奏**。

### 2.3 速度只管节奏,执行延迟单独算

- **一个动作打完后排到多远,只由速度决定**,不由这回合打了什么牌决定。
- 执行牌的"延迟 N 格才生效"是**预告窗口**,是另一码事,不参与"下次几时轮到我"的计算。
- （✅ 已实装：回合结束按 `Speed + 过载次数` 重排,节奏与延迟已分开。）

---

## 3. 卡牌类型：只有两种

旧文档写了三种(即时/执行/迅捷),实际收敛成**两种**：

| 类型 | 含义 | 归属 |
|---|---|---|
| **执行** | 打出时只扣资源、不结算,挂到执行轨,等 `ExecutingCost` 格后才生效;延迟期内可能被打断。**结算完不开新回合**（正常等自己的 Speed 轮转）。 | **法师为主**,战士可有少量执行大动作 |
| **即时**（≡迅捷） | 原地打、立刻结算、不进执行轨,能量没花完就继续打。 | 其他所有人 + 法师的廉价支援 |

> ✅ 执行线（07-10 已实装）：玩家执行牌=「**引线**」虚拟单位挂进 ATB 公共回合时钟（与天气同原语），挂起回合+`ExecutingCost` 的绝对回合结算,行动顺序轴上以卡牌图标可见;同回合结算序=天气→引线→我方→敌方。施法者结算前倒下则引线作废;敌人主动打断玩家引线**留待实测**（当前无敌技配此能力）。多发引线可同时在轨,每人每回合仍限挂 1 张。

- **为什么"即时"和"迅捷"合并**：在"能量花完才结束回合"的引擎下,没有任何牌会提前结束你的回合,所以旧"迅捷=保留回合"的特殊性消失了——两者都只是"立刻打"。真正有区别的只有**执行的延迟**。
- **执行轨不是只为法师服务**:敌人在这条轨上预告攻击,战士靠打断去干扰它,游侠靠闪避去躲它。**只有"玩家侧产出执行牌"这件事是法师专属**。

---

## 4. 三职业

### 4.1 法师 · 延迟工具箱（最慢）

- 一整套强力的**情境答案**牌(驱散/定身/沉默/护罩/位移/点杀…),而且**大多是执行类型**——延迟、预告、强。
- "打得少"来自**高费 + 每回合限挂 1 张执行**;手牌 5 张可以把答案攒在手里,等对的时机再掏。
- 核心手感:预告 → 保护自己这一发 → 掐时机。（07-10 拍板：~~执行结算后立刻开新回合~~ 作废——结算完不开新回合,正常按 Speed 轮转。）
- 副主题:抽牌(挖对的工具)＋ 廉价即时 buff/debuff 支援。
- 站位:最想待后排,最怕 `Root` 被拖出安全区。

### 4.2 战士 · 守护锚点（居中）

- **以即时牌为主**,可有少量执行大动作（07-09 拍板「可以多执行」,已入池:蓄力重击/地裂斩）。靠效果拉开身份:
  - **嘲讽**:改写敌人索敌到自己,替队友扛,省得法师/游侠回回走位。
  - **护甲**:续航,配合 `Dexterity`。
  - **打断**:用 `破韧 Stagger` / `格挡 Block` 打断敌人大招——队里唯一稳定的打断点。
- 定位:防御与秩序核心,最好上手,但有明确身份(不是白板)。

### 4.3 游侠 · 机动引擎（最快）

- 全部是**即时**牌 + 大量**移动**。主题是“移动为轴，同时喂养战场调度与士气”：
  - 通过卡牌移动友方角色 → 获得 **士气 `Morale`**。
  - 移动牌可以附带 **闪避 `Dodge`**、护甲或生成飞刀。
  - 士气 → 所有角色都可持有；使用伤害牌时消耗全部层数，每层使该牌每一段基础伤害 +2，或由特定卡牌转化为团队防御。
  - 循环：`移动友方角色 → 积累士气 → 用飞刀/攻击或防御牌兑现`。
- 定位：高速的战场调度者；可以补充伤害，但爆发需要先用移动建立士气。
- 引擎化：基础移动人人有；游侠每当通过卡牌成功移动一名友方角色时，便通过初始百相使其获得 1 层士气，再依靠更多移动牌与移动触发把战场调度转化为资源。

### 4.4 战士 vs 游侠 靠什么区分？

两个都是"廉价即时牌",**类型分不出他们**,区别**完全在效果词汇 + 池子/节奏**：

| | 战士 | 游侠 |
|---|---|---|
| 效果词汇 | 护甲 / 嘲讽 / 破韧 / 格挡 / 打断 | 移动 / 士气 / 闪避 / 飞刀 |
| 池子×节奏 | 中池、中频、原地站桩 | 小池、高频、满场穿梭 |

---

## 5. 公用系统

### 5.1 过载（透支阀门）——双通道（07-10 按代码现状定案）

- 代价统一:**下次行动重排时额外 +N 回合**（本回合过载计数累加,回合结束落账）。
- **通道一·能量透支（按钮型）**:能量不足时透支多打牌,**每人每回合限 1 次**。
- **通道二·卡牌自带 [过载]N 费用**:牌把过载印在自己身上（如 推手[过载1]、杀戮时刻[过载3]、千斤坠[过载1]、时间透支[过载2]）,**不占透支额度、可叠加**——同回合多张过载牌 = 代价累计。
- 定位:通道一是保底阀门,通道二是各职业的过载签名（游侠=透支爆发 / 战士=过载换甲 / 法师=时间透支）。叠加暂不封顶,实测若失控再加刹车。

### 5.2 两区站位

- **前排 / 后排**两个区,每区能站多名英雄。
- 敌人意图**预告目标区**(重击前排 / 扫射后排 / 全体…),玩家据此反复重排站位,把脆皮挪出被瞄准的区。
- 射程做**软**:大多数卡两区都够得着;站位主要是**防御性博弈**,不搞近战/远程硬锁。
- 摩擦(否则无脑最优):移动要花能量 + 非游侠笨拙;必要时再加区容量上限、少数敌人反走位(`Root`/打人数最少的区)。

### 5.3 移动

- **基础移动人人有**:每人每回合可花能量挪自己一步(不占卡),保底自救。
- **移动牌**:牌库里也有移动牌(走一步 + 带触发)。
- 移动模型 = **自己独立进出某个区**(现引擎已改成这样,不再和队友换位)。

  > ⚠️ 这里和 v1 有出入:v1 想要"相邻交换、牵动队友、挡刀博弈";现引擎是"自己单独移到某区,不牵动别人"。**独立移动更契合"一区多人"的两区模型**,但 v1 那种"换位暴露队友"的戏剧性没了。**需要你拍板留哪个。**

### 5.4 百相（签名天赋）

- 定义 build 的天赋系统,升级三选一(数据源 `#UpgradeOptions`)。
- 每角色开局自带一条招牌百相。例:
  - 游侠：每当自己通过卡牌成功移动一名友方角色时，使其获得 1 层士气；友方角色包括自己。
  - 法师:允许同回合挂 2 张执行牌。

---

## 6. 起手卡组的设计约束（写卡时照这个来）

给每个职业一套约 10 张的起手牌组,遵守:

| 职业 | 能量池 | 典型每回合 | 卡费区间 | 主类型 | 招牌效果 |
|---|---|---|---|---|---|
| 法师 | 4 | 打 1 张大的(＋1 张小支援) | 执行牌 3~4,支援 0~1 | 执行为主 | 情境答案 / 点杀 / 控制 |
| 战士 | 3 | 打 2 个稳的 | 1~2 | 全即时 | 护甲 / 嘲讽 / 打断 |
| 游侠 | 2 | 移动＋甩若干刀 | 移动付费,攻击 0 费 | 全即时 | 小刀 / 毒 / 闪避 / 移动触发 |

---

## 7. 执行线待办（audit 结果,不阻塞设计）

（07-10 对账更新）原六项已全部还清：✅闪避消费（受击免伤+客观回合衰减）✅破韧/格挡被动打断（挂敌人吟唱期,打够即断）✅嘲讽重定向 ✅过载=+N回合 ✅按速度重排+速度数值落表 ✅抽牌5。当前待办：

1. ~~玩家执行轨真延迟~~ → **✅ 已实装（07-10）**：引线=ATB 虚拟单位,真延迟、结算不开新回合、施法者死亡作废、轴上可见。遗留子项:①敌人打断玩家引线（留实测,需要时给敌技加打断 effect）;②法师「引线」组卡牌（引爆/蓄能/超频吟唱/顺延成长）的 effect 本身仍未做——但地基已就位,见 [[卡组设计_法师战士主主题]] §3;③`BattlePredictor` 不感知引线（与天气同,预览近似）。
2. **百相开局自带**——角色表通过 `Trait` 配置初始百相：战士 `FirstPushFree`、法师 `DoubleExecution`、游侠 `MoveAllyGrantMorale`。三者均已有对应战斗逻辑；通用的可组合百相框架仍待扩展。
3. **主动打断牌**——`InterruptEffect` 未进 schema、`InterruptCommand` 是死代码。**暂缓**（07-10 拍板:先用破韧/格挡的被动打断,实测后再看要不要主动版）。
4. **死 buff 清理**——`Chill`（冰冻）/`DrawCard`（灵感）表里有配置有 tooltip 但无消费代码,要么实装要么从表里摘掉。
5. **定价重跑**——速度改 法4/战3/游2 后,[[起手卡组设计_费用模型对齐]] §1 预算全部要重算。

---

## 8. 仍待你拍板的点

- **§5.3 移动模型**:独立移动(现状) vs 换位挡刀(v1)——留哪个?（仍未拍板）
- ~~§4.2 战士要不要执行牌~~ → **已拍板（07-09）：可以有**,蓄力重击/地裂斩已入池。
- ~~各职业具体 10 张~~ → 起手三套已落地（[[起手卡组设计_费用模型对齐]]）;法师/战士卡池已按主主题扩至 Irene023/Rocket018（[[卡组设计_法师战士主主题]]）。
- **过载叠加要不要封顶**（§5.1 通道二）——留实测。
