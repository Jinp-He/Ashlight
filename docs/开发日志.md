# 开发日志

> 按日期倒序记录每天的主要进展。设计基准见 [职业设计_三职业与公用系统](职业设计_三职业与公用系统.md)，待办见 [TODO](TODO.md)。

---

## 2026-07-04

### 三职业设计共识（grillme 拷问产出）
- 定稿 [职业设计_三职业与公用系统.md](职业设计_三职业与公用系统.md)，作为后续做卡表/敌人索敌/百相的基准。
- **公用系统**：能量制（高费=少张）/ 过载（每人每回合1次，代价=下次行动后退1~2格）/ 两区站位（敌人意图预告目标区→走位躲）/ 百相签名天赋 / 卡类型(即时·执行·迅捷)。
- **三职业三角**：战士＝守护者anchor(嘲讽/护甲/打断，前排) | 法师＝延迟执行工具箱(后排,结算即得新回合,怕Root) | 游侠＝机动引擎(移动→闪避+小刀→毒，utility非DPS)。
- 记忆：`project_battle_system_option3` 标记为已过时（重构前旧系统）。

### 敌人技能「目标分区」索敌系统（第一步落地）
主题「两区走位」的地基。分数据 + 代码两层：

**数据层**（Luban 导表）
- `__enums__.xlsx` 新增枚举 `TargetZoneEnum`：`Front / Back / Any / Conditional`。
- `#EnemySkillInfo.xlsx` 新增列 `TargetZone`；14 条敌技按描述填好分区（近战→Front / 远程·控制→Back / 全体AoE·自身buff→Any）。
- 正交设计：`TargetType`(单/群) × `TargetZone`(分区) 组合，如 `AllEnemy+Front`=群扫前排。
- 导出 `TargetZoneEnum.cs` + `EnemySkillInfo.cs`(+TargetZone字段) + `enemy_tbenemyskillinfo.json`。

**代码层**（索敌真正生效）
- 新建 `Core/Data/ZoneTargeting.cs`：`FilterByZone(units, zone)` 统一分区过滤，前/后区筛选，Any/Conditional 不过滤，空区暂回退全体。
- `DamageCommand` 加 `TargetZone` 字段，AOE 扩散按区过滤（玩家卡默认 Any 不受影响）。
- `EnemySkillToTimelineConverter` 把 skill 的 `TargetZone` 传进 `DamageCommand`。
- `BattleManager.TryPickEnemySkillAndTarget` 载体目标改为按分区选。
- **动态索敌**（关键）：`BattleManager.ResolveExecutionTarget`——目标在「执行那一刻」按当前站位重解：原目标仍在区内则继续打、被移出/死亡则改选该区其他人；AOE 本就在执行时现算。→ **移动躲伤害真正生效**。

**下一步**：意图 UI 表达目标区（让玩家看得出敌人打哪区）；嘲讽覆盖索敌；Conditional 规则；空区改真 miss。

### 移动牌 / 游侠百相 / 过载代价（第二批）
- **每回合注入移动牌**：新增 `Irene000/Rocket000/Zhouzhou000`（Swift·虚无+消耗，克隆自 Zhouzhou001，Energy=1）；`BattleManager.StartPlayerTurn → InjectBasicMoveCard` 按角色枚举名注入手牌，回合末/打出自动清除不污染牌库。
- **游侠百相 FirstMoveFree**：`CharaterInfo` 加 `Trait` 字段（Zhouzhou=FirstMoveFree）；`UnitState.FreeMoveUsedThisTurn` 每回合重置；`IsFreeMoveForOwner` 判定本回合首张「带移动」牌费用置 0、用后置标记。
- **过载代价（离散化）**：改为「本回合过载过 → 结束回合重排 +1 格」（直接体现在行动顺序视图的后移）。触发＝能量不足时自动透支（每人每回合 1 次），`OverloadCountThisTurn` 每回合重置。旧 0–100 负债仅剩敌人路径。
- **⚠️ 并发重构对齐**：本会话中 `UnitState.RowPosition` 字段被移除、改由 `BattleStateSnapshot.GetRowPosition`（列表索引0=前排）推导；相应把上一批的 `ZoneTargeting` 从读字段改为传 state 现算，3 个调用点同步。
- **待验证**：以上均未在 Unity 编译验证（改动量大且跨并发编辑文件），需 Unity 编译一次；`ZoneTargeting.cs` 等新文件 `.meta` 待生成。

### Bug 修复（第三批）
- **飞刀打不出**：`CardRuntimeState` 加 `OwnerCharacterId?`（生成时动态归属）；`AddToHandCommand` 按生成者写 owner；`CardViewController.GetOwnerCharacterId` 优先读实例 owner。
- **游侠百相 UI 无效**：`BattleManager.GetEffectiveEnergyCost`；卡面费用显示 + 能量门禁改用有效费用（首张移动显示0、0能量也能打）。
- **抽牌牌不抽牌**：`DrawCard` buff 从未被消费。正式改成 `DrawEffect`（新 effect 类型，defines.xml + 导表 + `DrawCommand`）——即时抽牌、不贴 buff；Irene004 迁移为 `DrawEffect,F,1`。

### 前后排显式化 + 移动指令重做
- **两容器 UI**：`PlayerFrontRow`/`PlayerBackRow`，按 `RowPosition` 生成分流 + `LayoutRebuilder.ForceRebuildLayoutImmediate` 强制重排（HLG 加子物体不自动布局）。
- **位置模型转变**：从「list索引0=单前排+换位」→ **`UnitState.RowPosition` 显式字段 + 两区可多人**；`GetRowPosition` 读字段。开局战士前排、其余后排。
- **移动指令**：`MovePositionCommand` 从「和目标换位」→「**把自己移到目标区**」（`mode`: FrontRow/BackRow/Toggle → `BattleRowPosition`）；移动牌 TargetType 全改 Self；UI `OnPositionSwapped` 支持单角色移动重挂容器。

### 今日剩余（未做）
1. **意图显示**：让敌人「攻击前排/后排」在意图 UI 上更明显（当前 `TargetZone` 数据有了、索敌也生效，但玩家看不出打哪区）。
2. **修改卡牌设计**：按三职业设计调整卡表。
